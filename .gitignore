# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

/src/generated/prisma

# local sqlite database (dev)
/prisma/*.db
/prisma/*.db-journal

# prisma-generated agent skill docs
/.agents

# prisma-generated editor skill docs / lockfile (keep only .claude/launch.json)
/.claude/skills
/.windsurf
skills-lock.json

# migration SQL must be tracked (overrides global *.sql ignore)
!/prisma/migrations/**/*.sql

# our application code — override a global `lib/` ignore that would otherwise
# swallow the entire src/lib directory
!/src/lib/
!/src/lib/**

# ship the example env (real .env stays ignored via .env*)
!/.env.example
